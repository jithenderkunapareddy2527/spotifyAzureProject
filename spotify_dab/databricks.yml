bundle:
  name: spotify_dab
  uuid: 903a4c6a-c871-4e0a-b11c-ca7c31474e55


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://adb-7405616810182377.17.azuredatabricks.net
      presents:
        source_linked_deployment: false
  prod:
    mode: production
    workspace:
      host:  http://adb-7405616810182377.17.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/jithender154@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: jithender154@gmail.com
        level: CAN_MANAGE
